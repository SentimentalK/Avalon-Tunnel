# Avalon Tunnel - Python Dependencies
# 共享依赖：CLI (main.py) 和 API (api_server.py) 都使用

# API 服务器
fastapi==0.115.0
uvicorn==0.32.0

# 数据验证
pydantic==2.9.0
email-validator==2.1.0

# 环境变量
python-dotenv==1.0.0

# SQLite 是 Python 内置的，不需要额外安装